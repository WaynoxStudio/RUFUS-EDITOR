<Window x:Class="RufusMapEditor.App.SaveMapIdWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Guardar mapa"
        Width="400" Height="220"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource WindowBackground}"
        Foreground="{DynamicResource TextPrimary}">
  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    <TextBlock x:Name="PromptText" TextWrapping="Wrap" Margin="0,0,0,12"
               Foreground="{DynamicResource TextPrimary}"/>
    <TextBlock Grid.Row="1" Text="Map ID" Margin="0,0,0,4"
               Foreground="{DynamicResource TextPrimary}"/>
    <TextBox x:Name="MapIdBox" Grid.Row="2" FontFamily="Consolas"
             Foreground="{DynamicResource TextPrimary}"
             Background="{DynamicResource InputBackground}" BorderBrush="{DynamicResource InputBorder}"/>
    <TextBlock Grid.Row="3" TextWrapping="Wrap" FontSize="12" Margin="0,8,0,0" VerticalAlignment="Top"
               Foreground="{DynamicResource TextSecondary}"
               Text="Se creará Library\Maps\&lt;ID&gt; con .rufmap, .png, _MapData.txt y _AME.swf (si Flasm está disponible)."/>
    <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
      <Button Content="Cancelar" Width="90" Margin="0,0,8,0" Padding="8,4" IsCancel="True" Click="Cancel_Click"/>
      <Button Content="Guardar" Width="90" Padding="8,4" IsDefault="True" Click="Ok_Click"/>
    </StackPanel>
  </Grid>
</Window>
