<Window x:Class="RufusMapEditor.App.LangRemoteSyncWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="LANG remoto"
        Width="520" Height="420"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource WindowBackground}"
        Foreground="{DynamicResource TextPrimary}">
  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <Border Background="{DynamicResource DialogHeaderBackground}" CornerRadius="6"
            BorderBrush="{DynamicResource Border}" BorderThickness="1" Padding="12,10" Margin="0,0,0,12">
      <StackPanel Orientation="Horizontal">
        <Border Width="3" Height="16" CornerRadius="1.5" Background="{DynamicResource BrandAccent}" Margin="0,0,10,0"/>
        <TextBlock Text="LANG REMOTO · sincronización READ-ONLY" FontWeight="SemiBold" VerticalAlignment="Center"/>
      </StackPanel>
    </Border>

    <TextBox x:Name="SummaryBox" Grid.Row="1" IsReadOnly="True" TextWrapping="Wrap"
             FontFamily="Consolas" FontSize="12" VerticalScrollBarVisibility="Auto"
             AcceptsReturn="True"/>

    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
      <Button x:Name="SaveCopyButton" Content="Descargar copia local" Padding="10,4" Margin="0,0,8,0"
              IsEnabled="False" Click="SaveCopy_Click"/>
      <Button Content="Cerrar" Padding="10,4" IsCancel="True" IsDefault="True"/>
    </StackPanel>
  </Grid>
</Window>
