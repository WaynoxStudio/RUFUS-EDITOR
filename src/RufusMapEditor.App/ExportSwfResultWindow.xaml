<Window x:Class="RufusMapEditor.App.ExportSwfResultWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Exportación SWF"
        Height="360" Width="520"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource WindowBackground}"
        Foreground="{DynamicResource TextPrimary}"
        ResizeMode="NoResize">
  <DockPanel Margin="16">
    <Button DockPanel.Dock="Bottom" Content="Cerrar" Width="100" HorizontalAlignment="Right"
            Click="Ok_Click" Margin="0,12,0,0" Padding="8,4"/>
    <ScrollViewer VerticalScrollBarVisibility="Auto">
      <TextBlock x:Name="SummaryText" TextWrapping="Wrap" FontFamily="Consolas" FontSize="12"/>
    </ScrollViewer>
  </DockPanel>
</Window>
