<Window x:Class="RufusMapEditor.App.RevisionInputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Revisión"
        Width="440" Height="220"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource WindowBackground}"
        Foreground="{DynamicResource TextPrimary}">
  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    <TextBlock x:Name="PromptText" TextWrapping="Wrap" Margin="0,0,0,12"/>
    <TextBlock Grid.Row="1" Text="Revisión nueva" Foreground="{DynamicResource TextSecondary}" Margin="0,0,0,4"/>
    <TextBox x:Name="RevisionBox" Grid.Row="2" FontFamily="Consolas" VerticalAlignment="Top"/>
    <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
      <Button Content="Cancelar" Width="90" Margin="0,0,8,0" Padding="8,4" IsCancel="True" Click="Cancel_Click"/>
      <Button Content="Continuar" Width="90" Padding="8,4" IsDefault="True" Click="Ok_Click"/>
    </StackPanel>
  </Grid>
</Window>
