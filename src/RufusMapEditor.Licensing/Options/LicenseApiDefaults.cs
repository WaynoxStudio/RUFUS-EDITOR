namespace RufusMapEditor.Licensing.Options;

/// <summary>Shared production origin for license HTTPS (same host as AiBackend).</summary>
public static class LicenseApiDefaults
{
    /// <summary>Confirmed LIC.4 VPS origin — no trailing slash.</summary>
    public const string ProductionBaseUrl = "https://vmi3502135.contaboserver.net";
}
